% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrr_prep_survey_vars.R
\name{mrr_prep_survey_vars}
\alias{mrr_prep_survey_vars}
\title{Prepare Survey Variables for Analysis}
\usage{
mrr_prep_survey_vars(df, var_tag, relabel = TRUE)
}
\arguments{
\item{df}{A survey data frame containing RespondentID and labelled variables}

\item{var_tag}{Character string prefix to match column names (e.g., "q0002")}

\item{relabel}{Logical; if TRUE (default), rename columns using their variable
labels. If FALSE, keep original column names.}
}
\value{
A data frame with RespondentID and selected variables, optionally relabeled
}
\description{
Selects specific columns from a survey data frame based on a variable tag prefix,
removes character columns (typically open-ended comments), and optionally
relabels columns with their variable labels from the SPSS metadata.
}
\examples{
\dontrun{
# Select and relabel all q0002 variables
df_selected <- mrr_prep_survey_vars(m26, "q0002")

# Select without relabeling
df_raw <- mrr_prep_survey_vars(m26, "q0002", relabel = FALSE)
}

}
