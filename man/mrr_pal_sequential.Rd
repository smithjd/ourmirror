% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mrr_palettes.R
\name{mrr_pal_sequential}
\alias{mrr_pal_sequential}
\title{Sequential brand ramp}
\usage{
mrr_pal_sequential(hue = "Green", n, blend = "white", direction = 1)
}
\arguments{
\item{hue}{Name of a brand hue from \code{\link[=shambhala_palette_function]{shambhala_palette_function()}}
(e.g. "Green", "Blue", "Crimson", "Yellow"), or a hex colour string to ramp
from directly (covers the brown centre-size ramp). Default: "Green".}

\item{n}{Number of steps in the ramp.}

\item{blend}{\code{"white"} to lighten toward white (default), or a brand hue name
/ hex colour to blend the hue toward.}

\item{direction}{1 for the natural order described above (default), -1 to
reverse it.}
}
\value{
A character vector of \code{n} hex colours.
}
\description{
Builds a sequential ramp from a single brand hue using
\code{monochromeR::generate_palette()}. Use for ordered magnitude — size tiers,
cohort funnels, retention gradients.
}
\details{
Two ramp styles via \code{blend}:
\itemize{
\item \code{blend = "white"} (default) lightens the hue toward white and returns the
ramp light -> dark, so the deepest tier is the pure brand hue.
\item \verb{blend = <hue name or hex>} blends the hue toward another colour and returns
the ramp hue -> blend (e.g. \code{blend = "Grid"} fades green into the brand
gridline gray). This is the behaviour the legacy \code{\link[=mrr_color_steps]{mrr_color_steps()}} wraps.
}
}
\examples{
mrr_pal_sequential("Green", 5)
mrr_pal_sequential("#7A5C3E", 5)
mrr_pal_sequential("Green", 4, blend = "Grid")

}
